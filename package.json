{
  "name": "greenie-culture-root",
  "version": "1.0.0",
  "description": "Root for Greenie Culture Project",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others -n \"BACKEND,FRONTEND\" -c \"bgBlue,bgGreen\" \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}